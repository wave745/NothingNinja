# netlify.toml
[build]
  command = "npm run build"
  publish = "dist"
  
# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  
# Environment variables for the frontend
[build.environment]
  NODE_VERSION = "20"